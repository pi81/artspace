/** Active tenant for this deployment — retarget `@tenant/*` in tsconfig to switch projects. */
export { buildGallerySystemPrompt } from "@tenant/assistant-config";
export { galleryContentRetriever } from "@tenant/content-retriever";
export { GalleryChatWidget } from "@tenant/GalleryChatWidget";
